# M3: 内容与系统扩展（增量层）

**目标**：在 M2 核心循环上，补齐全部能力 + **5 关** + 评分 + 腐败视效，把 demo 从「1 关可玩」扩到「5 关完整 + 4 星评分」。

**性质**：**增量 Should**。M2 完成后推进；时间紧按依赖 DAG 保核心能力先于视效。

**完成标准（DoD）**：5 关全可玩（**含关 1 教学**）、5 能力全实现、4 星评分跑通、腐败视效到位。

**前置**：M2 完成

**依赖 DAG（O3）**：
```
3.0(无前置) ──┐
M2.1 ──→ 3.1 ──→ 3.3
M2.1 ──→ 3.2
M2.1 ──→ 3.4
M2.5 ──→ 3.5
3.0 + 3.1~3.4 ──→ 3.6 ──→ 3.9
M2.8 ──→ 3.7
M2.6 ──→ 3.8
```

---

## 任务清单
- [~] 3.0 纸面可解性证明（关 1 / 3 / 4 / 5）⏸搁置（队友做关卡中，障碍布局未知；待关卡定后补，或并入 3.6 白盒验证）
- [x] 3.1 推动完整系统（磁力枪·抓取搬运型）
- [x] 3.2 拉动能力（磁力枪·牵引）
- [x] 3.3 推动二选一：牵引光束型
- [x] 3.4 冲刺能力
- [ ] 3.5 跳跃超载飞行状态机（完善）
- [ ] 3.6 关 1 / 3 / 4 / 5 白盒
- [ ] 3.7 评分系统（4 星）
- [ ] 3.8 腐败 URP 视效
- [ ] 3.9 场景主题（至少 3 个）

---

## 任务详述

### 3.0 纸面可解性证明（关 1 / 3 / 4 / 5）—— ⏸ 搁置（2026-06-20）
> **搁置原因**：队友在做关卡（TestLevel 分支），关卡实际障碍布局未知，纸面证明前提不稳。M3 先做 player 能力（3.1–3.5）。待关卡定后补 proof，或可解性并入 3.6 白盒 + playtest 验证（死局逃生口仍需保留）。

- **做什么**：**纯设计推演，不写代码**。每关产出一份 `docs/design/level_X_solution_proof.md`：障碍清单 × 所需能力 × 至少 2 个可解配置。重点证明：
  - **关 4 双解法可解**：至少一个 `(移动4 + 冲刺超载 + 跳/推/拉中恰 2 个)` 配置可解（`§J1`）
  - **关 5 代表性弃法可解**：验 3–4 种典型弃法（如弃后退 / 弃冲刺 / 弃拉动）可解（`§J1`、**R3 决策**）
- **依据**：`§J1`（关 4/5 可解性）、`§J4`；**R2（开发前证明，非白盒后）、S1（产物定义）、R3（代表性弃法）**
- **为什么前置**：在写关卡代码前消除死局风险，0 代码成本，回报最高（避免白盒返工）
- **验收**：4 份 proof 文档完成，关 4/5 可解性有明确结论

### 3.1 推动完整系统（磁力枪·抓取搬运型）
- **做什么**：把 M2.1 的最简推动升级为完整能力——磁力枪·抓取模式，近距吸附搬运；超载 = 同时搬多个。
- **依据**：GDD `4.2`（推动·搬运型）；`§B`、`§A` 推荐
- **验收**：吸附搬运 + 超载搬多个
- **依赖**：M2.1

### 3.2 拉动能力（磁力枪·牵引）
- **做什么**：拉动——发射磁铁黏目标 → 再按吸向自己；超载 = **反向把自己吸向目标（抓钩 / 自身位移）**。（v1.0 恢复抓钩，撤销 §J3；俯视 2.5D 下调试可接受。**fallback**：进度告急则降级为拉更远/多个，关 3 解法 B 同步改为「拉石柱更远腾路」）
- **依据**：GDD `4.2`；`§B`、`§J3`
- **验收**：拉物体（吸向自己）+ 超载抓钩（把自己吸向目标，v1.0）
- **依赖**：M2.1

### 3.3 推动二选一：牵引光束型
- **做什么**：推动第二种形态——牵引光束远距推物；超载 = 推更远更快（数值）。配置阶段与搬运型二选一（用 M2.4 预留的 `ability.variants`）。
- **依据**：GDD `4.2`；`§B`；**R7（依赖 M2.4 接口预留）**
- **验收**：配置时能选光束型 + 光束推物 + 超载数值
- **依赖**：3.1

### 3.4 冲刺能力
- **做什么**：冲刺 = dash；超载 = dash×2。关 4 引入。
- **依据**：GDD `4.2`；`§B`
- **验收**：冲刺位移 + 超载连 dash 两次
- **依赖**：M2.1

### 3.5 跳跃超载飞行状态机（完善）
- **做什么**：把 M2.5 的瞬时短飞升级为**持续飞行状态机 + 缓落 + SO 调参**（时长/手感可调）。
- **依据**：GDD `4.2`；`§B`、`§H`；**H3（与 M2.5 划界：本任务做状态机+缓落+SO，M2.5 只做瞬时写死版）**
- **验收**：飞行时长可调 + 缓落手感可接受
- **依赖**：M2.5

### 3.6 关 1 / 3 / 4 / 5 白盒
- **做什么**：白盒搭建：
  - **关 1（教学·苏醒）**：纯移动走到出口，onboarding（**H1 补位**）
  - 关 3（拉动·信号干扰）
  - 关 4（冲刺·断桥·双解法）
  - 关 5（终局·双超载·必弃 1）
- **依据**：GDD 第 5 章 + 第 6 章；`§D`；**3.0 纸面证明（关 4/5 可解性已验）**
- **关键（防认知偏差）**：
  - 关 4 双解法必须可解（引用 3.0 proof）
  - **关 5「代表性弃法可解」**（3–4 种典型，引用 3.0 proof，**R3 决策**；不再要求「任意弃 1」）
- **验收**：4 关各自可解路径跑通
- **依赖**：3.0 + 3.1–3.4

### 3.7 评分系统（4 星）
- **做什么**：4 星（通关 / 时间 / 节制 / 精简）+ 软计时 + HUD + 结算界面（评分挂载点）。**每关节制阈值独立 SO 字段**（关 4 可独立放宽，**R6**）。
- **依据**：GDD `4.7`；`§F`；`§J4`；**R6**
- **不做**：关卡选择界面（M4.3，P2）
- **验收**：过关结算显示星级；4 星可追
- **依赖**：M2.8

### 3.8 腐败 URP 视效
- **做什么**：腐败 5 层级（GDD 4.4）——视效（泛红 / glitch 抖动 / 屏幕抖动，URP Volume）+ 音效（不和谐干扰）+ **机制层**（层级 2 移速降、层级 3 能力冷却延长）。机制层涉及角色控制器 + 能力系统。
- **依据**：GDD `4.4`；支柱 4
- **不做**：机关反转 / 重力波动（v1.0 已移除，改本任务的层级设计）
- **验收**：腐败层级有清晰视觉/听觉反馈
- **依赖**：M2.6

### 3.9 场景主题（至少 3 个）
- **做什么**：至少 3 个可复用户外场景主题（坠毁点 / 荒原 / 古代遗迹 / 母舰残骸中选 3+，具体待美术）。**全室外**。
- **依据**：GDD `2.3`；`§D`；**S5（与 GDD §9「4 主题」统一为「至少 3 个」，GDD 同步降措辞）**
- **验收**：5 关有差异化场景外观
- **依赖**：3.6

---

## 测试记录

### 3.1 推动完整系统（磁力枪·抓取搬运，2026-06-20）
- 测试：**MCP 验证**（编译零错 + play 反射模拟）→ 核心状态机通过
  - 抓取：按 E（Idle→Carrying），TestCrate 进 _carried，物体到玩家距离=1.50（=carryDistance，吸附前方正确）
  - 释放：再按 E（Carrying→Idle），_carried 清空
  - Trigger=Continuous / Overload=MultiTarget / PushTuning 新字段就位
- **遗留（手动 playtest）**：完整手感（按键时机 / 多箱横向排列 / 超载范围内搬多个 / 释放落点）→ `docs/tests/M3-手动验证清单.md`

### 3.2 拉动能力（磁力枪·牵引 + 超载抓钩，2026-06-20）
- 测试：**MCP 验证**（编译零错 + play 反射模拟）→ 核心功能通过
  - 普通拉：按拉动键，前方 grabRange 内最近 Pushable 吸到玩家前方 pullToDistance（水平距=1.5）
  - 超载抓钩（v1.0）：射线命中 Pushable → 玩家瞬时位移到命中点前方 hookLandDistance（pAfter -6→-2，落 crate 前）
  - RaycastAll 跳过玩家自身 collider；CharacterController disable/set/enable 防 teleport 抖动
  - AbilityKind 加 Pull；IAbilityContext 加 Controller（位移能力需）
- **遗留（手动 playtest）**：完整手感（拉/抓键时机 / 抓钩落点纵深 / 超载射程边界）→ `docs/tests/M3-手动验证清单.md`

### 3.4 冲刺能力（dash + 超载 dash×2，2026-06-20）
- 测试：**MCP 验证**（编译零错 + play 反射模拟）→ 核心功能通过
  - 普通 dash：向移动输入方向瞬时突进 dashDistance（位移=5.00）
  - 超载 dash×2（J4）：距离×2（位移=10.00），1 次触发算 1 次腐败
  - RaycastAll 防穿墙（跳过自身）；Teleport 模式；冷却 cooldown
  - AbilityKind 加 Dash
- **遗留（手动 playtest）**：完整手感（方向输入时机 / 防穿墙边界 / 冷却节奏）→ `docs/tests/M3-手动验证清单.md`

### 3.3 推动二选一·牵引光束型（2026-06-20）
- 测试：**MCP 验证**（编译零错 + play 反射模拟）→ 核心功能通过
  - 射线推：按 PushBeam，前方 beamRange 内 Pushable 被推 beamPushDistance（TestCrate z 0→3）
  - RaycastAll 跳过玩家自身；超载 ×倍率（ChargeOverload 一次）
  - 路线 A 扁平：PushBeam 独立 AbilityKind（与 Push 搬运二选一，关卡固定）；AbilityKind 加 PushBeam
- **遗留（手动 playtest）**：完整手感（射线命中边界 / 超载推距 / 关卡 variant 配置）→ `docs/tests/M3-手动验证清单.md`

## 备注
- **M3 当前优先级（2026-06-20 调整）**：先做 **player 能力 3.1–3.5**（推动/拉动/推动二选一/冲刺/飞行）；**3.0 纸面证明、3.6 白盒、3.9 场景主题后置**——队友在做关卡（TestLevel 分支），障碍布局未知；3.7 评分 / 3.8 腐败视效待 player 就绪后推进。
- **3.0 搁置**：队友做关卡中，关卡实际障碍未知；纸面证明待关卡定后补，或可解性直接并入 3.6 白盒 + playtest 验证（死局逃生口仍需保留）。
- 关 5 已从「任意弃 1 可解」放宽为「代表性弃法可解」（R3），GDD 同步改措辞。
- scope：3.1–3.4 能力优先于 3.8/3.9 视效/场景。

---

## M3 前置清理项（2026-06-20 M3 启动前代码审查，按任务归位）

> 本节登记「留到对应 M3 任务做」的架构性前置，做到该任务时先看这里。本次（feature/m3-content-expansion）已顺手做掉低风险卫生清理（删 PlayerControls 死代码 / 修 CorruptionHUD gradient 死读 / KeyMap 改 Enum.Parse / 删 SetOverloadActive 死方法 / MaxValue 抽 const / 删空 OnEnable），见 commit 27a8754。下列是**未做、留到对应任务**的项。

- **3.0 / 3.1 前置（必修）**：`ability.variants` 数据模型实际不存在——M2.md / toConfirm.md 三处声称「M2.4 已预留」，代码零命中，**M3.3（推动二选一）硬阻塞**。需定义 `AbilityVariantId`（Push_Carry/Push_Beam）+ 改 BindingItem/KeySlot/LevelConfigSO 携带变体 + LoadoutValidator 校验。务必在 3.1/3.3 开工前补。
- **3.1 / 3.2 前置**：`AbilityKind` 枚举仅 Move/Jump/Push，缺 `Pull`/`Dash`（IAbility.cs）。追加枚举值（顺序追加不动现有序号，保 .asset 反序列化）。
- **3.1 前置**：`PushAbility` 是 M2.1 最简版（瞬时非物理射线推），M3.1 重写为磁力枪搬运型；`PushTuning` 多字段标注「M3 调参」。
- **3.5 前置**：(a) 超载强化参数散落 3 个能力（Move/Jump/Push 共 5 字段），抽 `OverloadTuning` SO；(b) `PlayerAbilityController.Update` 把轮询/连续/重力/瞬时/Move 揉在一起，拆成 PollInput/TickContinuous/ApplyGravity/TickInstant/ApplyMove，为飞行状态机铺路。
- **3.6 前置（latent bug，单关未暴露）**：(a) `GameManager` DontDestroyOnLoad 跨场景存活，但事件订阅在 Start 做、`_corruption`/`_levelExit` 是新场景新实例 → 多关切换订阅丢失；(b) `EnterWon` 过关不重置腐败/玩家位置/`_verticalVelocity`；(c) `FallReset` 是 M2.7 简单版，M2.8 本应替换未替换。
- **3.7 前置**：(a) `GameManager.Start` 6 个 `FindObjectOfType`（deprecated，改注册模式或 `FindFirstObjectByType`）；(b) 三种进 Loadout 路径（Esc/满格/过关）语义不同却都走 `EnterLoadout()`，缺 `LoadoutReason`；(c) `LoadoutUIController._carryoverConfig` 死字段（恒 null，M2.8 carryover 未接通）。
- **3.8 前置**：(a) `KeySlotUI` 超载/常态配色硬编码，抽参数；(b) 腐败机制层（层级 2 移速降、层级 3 冷却延长）无干净接入点，依赖 3.5 的 Update 拆分 + OverloadTuning SO。
