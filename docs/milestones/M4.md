# M4: 打磨与深度（打磨层）

**目标**：把 demo 从「功能完整」打磨到「可玩闭环 + 手感/深度到位」——先查漏补缺（4.1 ✅），补齐多关场景核心系统接入（4.2 最高优先），再优化手感/腐败/数值，最后全 5 关 playtest 验证核心乐趣（4.9）。

**性质**：**打磨 Could**。M3 完成后推进；时间允许才做，否则作为后续迭代（scope 兜底的最外层）。

**完成标准（DoD）**：4.1 完整性检查通过 ✅；4.2 LevelPreview1-3 可玩闭环（多关推进+评分+腐败视效）；4.3/4.4/4.7 优化到位；4.9 全 5 关 playtest 双解法两条路线都有人走。

**前置**：M3 完成 ✅

**架构背景（2026-07-01 4.1 确认）**：关 1-5 全塞单一场景 `LevelPreview1-3`（最终 1-5），**靠 CheckPoints 区分关卡**（非多场景）。M3 的评分(3.7)/腐败(3.8) 代码在 SampleScene/Level_01 验证，**未接入 LevelPreview1-3**；多关推进机制（检查点切关/过关）是单关→多关架构升级的新缺项（4.2 补）。

---

## 任务清单
- [x] 4.1 Demo 完整性检查（出检查表 + 缺陷清单）
- [x] 4.2 LevelPreview1-3 多关机制 + 系统接入
- [~] 4.3 能力手感优化（场景+机关修复✅ / 手感调参待 playtest · **待测**——用户暂不便游玩）
- [ ] 4.4 腐败系统优化（系统博弈层）
- [ ] 4.5 配置阶段 3D 实时预览（P2 · 挂起，探索后判断暂不需要）
- [ ] 4.6 随机化效果（P2 · 挂起待讨论）
- [ ] 4.7 数值精调（平衡收口层）
- [ ] 4.8 结局（解谜化 + 动画）
- [ ] 4.9 全 5 关 playtest（最终验证）
- [x] 4.10 关卡机关系统接入（交互物修复✅：Button/Pillar/Pontoon/Pipeline 可交互；缺失机关几何归队友）

---

## 任务详述

### 4.1 Demo 完整性检查 ✅
- **做了什么**：以「玩法验证 demo」视角对 `LevelPreview1-3` 走查 6 维度，产出缺陷清单。完整性 ≠ 商品质素，聚焦：①核心循环 ②关1/2/3 可玩（关4/5 待队友）③双解法 ④评分腐败 ⑤菜单流程（搁置）⑥blocking bug。
- **依据**：玩法验证 demo 定位；GDD `7`
- **分工定位**：4.1 = 开发者视角冒烟（「能不能玩通」），可多次跑；4.9 playtest = 玩家视角乐趣验证（最后做）。
- **验收**：检查表覆盖 6 维度；缺陷清单按优先级排序。✅ 达成

### 4.2 LevelPreview1-3 多关机制 + 系统接入（最高优先）
- **做什么**：让 `LevelPreview1-3` 从「几何预览」变「可玩多关场景」。修 4.1 发现的 B+C 类：
  - **B 检查点切关机制**（新写）：写 `CheckPoint` 脚本（玩家碰触发器 → 通知 GameManager 切当前关 + 更新出生点）；GameManager 加多关状态（CurrentLevel + 按关切 LevelConfig）；当前 3 个检查点 collider **无脚本**不响应
  - **B 过关/切关触发**：关末出口（LevelExit 或末检查点联动）→ 切下一关；末关 → 通关（当前 `_levelExit=null`、无 LevelExit 对象）
  - **C 评分系统接入**：场景放 ScoreSystem/StarRating/ScorePopup（M3.7 代码在，场景缺），接 GameManager 过关事件（当前 `_scorePopup=null`）
  - **C 腐败视效接入**：配 Global Volume 的 VolumeProfile（Vignette + ColorAdjustments），接 CorruptionTracker 层级驱动（M3.8 代码在，当前 `sharedProfile=null`）
- **顺带**：检查点重命名（CheckPoint1/CheckPoint/CheckPoint 重名 → CheckPoint_Lv1/2/3）
- **依据**：4.1 缺陷清单 B/C 类
- **为什么最高优先**：demo 可玩的前提；没它 4.3/4.4 优化无的放矢
- **验收**：`LevelPreview1-3` 跑通 配置→玩关1→检查点切关2→…→过关→评分弹窗→腐败视效反馈 全闭环
- **依赖**：M3.6 / M3.7 / M3.8（代码在）；4.1 ✅
- **测试方式**：`MCP 验证` + `手动验证`

### 4.3 能力手感优化（操作反馈层）
- **做什么**：优化各核心能力（推动/拉动/冲刺/跳跃飞行）的**操作反馈**——输入响应、起跳/落地前摇后摇、推箱拉物力度曲线、dash 起停与镜头、飞行高度维持、动画/音效/镜头反馈衔接。
- **维度边界**：只管「感觉」，不动「平衡数」（归 4.7）。手感临时调的数值留给 4.7 收口。**顺带处理 4.1 发现的 BoxCollider 负 scale ×31**（Pillar_Trick 机关柱翻转，影响机关碰撞/双解法）。
- **依据**：GDD `3`；4.1 缺陷清单 A 类
- **验收**：核心能力手感自评合格 + BoxCollider 警告清零 + 4.1 缺陷对应项关闭
- **依赖**：M3.6 + 4.1 ✅
- **测试方式**：`手动验证` + `MCP 验证`

### 4.4 腐败系统优化（系统博弈层）
- **做什么**：优化腐败 5 层级的**系统博弈体验**——各层惩罚触发节奏、与超载模块博弈张力、层级间视效与反馈递进。**合并原「腐败层级微调」**。
- **维度边界**：管「系统体验与节奏」；具体惩罚数值最终标定归 4.7。最终校准留 4.9 playtest。
- **依据**：GDD `4.4`；M3.8；4.1 缺陷清单
- **验收**：5 层级惩罚递进手感自评合理；超载博弈有张力
- **依赖**：M3.8 + 4.1 ✅（且依赖 4.2 把腐败视效接入场景）
- **测试方式**：`手动验证` + `MCP 验证`

### 4.5 配置阶段 3D 实时预览（P2 · 挂起）
- **状态**：**挂起**（2026-07-02 探索后用户判断暂不需要）。
- **原计划**：配置阶段关卡预览升级为 **3D 实时预览 + 相机轨道控制**，玩家可旋转看布局——解决「配置阶段全屏黑底盲配」问题，强化「配置即解谜」。
- **探索结论**：LoadoutCanvas 全屏 85% 黑底盖死 3D；工作场景手工 Cam + PositionConstraint（Cinemachine 3.1.7 装了但场景零使用）；所谓「yaw 不跟随」是 CM TopDown 故意不挂 Aim（GDD 锁 2.5D 固定俯视）+ CameraYawOrbit 未部署，非 bug。技术方案已备（手工 Cam 临时挪 / Cinemachine 双 vcam / UI 半屏让位 / RenderTexture），见 `/home/awaken/.claude/plans/kind-napping-biscuit.md`，随时可重启。
- **重启条件**：用户认为「配置阶段看关卡」有必要时。

### 4.6 随机化效果（P2 · 挂起待讨论）
- **做什么**：[待讨论] 引入某种随机化效果增加重玩性或表现力。
- **当前状态**：**挂起**。与「键位解谜」确定性可推理核心冲突，scope 风险最大。待 4.2-4.4 完成后视时间与设计共识再定方向。
- **依据/验收/测试**：[待定]

### 4.7 数值精调（平衡收口层）
- **做什么**：全局数值**统一平衡收口**——腐败增量、强化倍率、跳跃距离、飞行时长、节制阈值、dash 语义、各能力冷却。汇总 4.3/4.4 为手感临时调的数值。
- **维度边界**：管「平衡数」，4.3/4.4 之后的收口。
- **依据**：GDD `7`；`toConfirm §H`
- **验收**：数值全局自洽、手感到位
- **依赖**：4.3 + 4.4 大致完成
- **测试方式**：`手动验证` + `MCP 验证`；最终校准留 4.9

### 4.8 结局（解谜化 + 动画）
- **做什么**：关 5 终点——「发送信号」能力绑键按下触发结局（键位配置主题收尾），通讯阵列点亮 + 信号射向星空动画。
- **依据**：GDD `2.5`；v1.0 整合
- **验收**：结局按键解谜 + 动画都实现
- **依赖**：M3.6（关 5 跑通，关 5 白盒待队友）；可与 4.7 并行
- **测试方式**：`手动验证` + `MCP 验证`

### 4.9 全 5 关 playtest（最终验证）
- **做什么**：对**全 5 关**玩家视角 playtest——关 1 onboarding、关 2/3/4 双解法两条路线、关 5 终局节奏。若双解法都走超载 → 调高常规繁琐度/腐败代价；都不走 → 反向调。
- **依据**：`toConfirm §J8`；GDD `7`；**S3**
- **为什么放最后**：4.2-4.4/4.7 优化到位后再 playtest 反馈更准；也依赖关 4/5 白盒（队友）就绪。
- **验收**：全 5 关 playtest 记录；双解法关两条路线都有人走
- **依赖**：4.2 + 4.3 + 4.4 + 4.7 + 关 4/5 白盒（队友）
- **测试方式**：`手动验证`（找人玩 + 记录）

---

## 测试记录

### 4.1 Demo 完整性检查 ✅（2026-07-01，LevelPreview1-3 场景）
- **测试**：`MCP 验证` → 已有部分完整
  - 编译 0 错 0 警告；missing script 0
  - Play 启动 → GameManager 进 Loadout + Player 有 LoadoutConfig + 腐败 0
  - Player 6 能力（Move/Jump/Pickup/Pull/Dash/Push）+ CorruptionTracker + FallReset + ProtagonistRigAnimator 齐全，SO 引用未断
  - 核心对象在位：GameManager/Player/Protagonist/Cam/EventSystem/Loadout&Corruption HUD/3 检查点 collider/关1-3 几何
- **缺陷清单（产出）**：
  - **A 真 bug**：BoxCollider 负 scale ×31（Pillar_Trick 机关柱/RockThin 翻转，P2 碰撞异常）；检查点命名重名（P3）
  - **B 多关机制待写**：检查点 collider 无脚本（碰检查点无响应）；过关/切关触发缺失（`_levelExit=null`，无 LevelExit 对象）
  - **C 系统接入待补**：评分对象全缺（M3.7 代码在，场景没放）；腐败视效 Volume profile 空（M3.8 代码在，`sharedProfile=null`）
  - **D 通过**：见上
- **核心结论**：LevelPreview1-3 = 几何+Player+能力+配置/HUD 就绪；多关推进机制待写、评分/腐败视效待接入。能「配置→进关→玩」，不能「过关→切关→评分→腐败反馈」
- **遗留**：B+C 转 4.2；A 的 BoxCollider 留 4.3 一并处理

### 4.2 LevelPreview1-3 多关机制 + 系统接入（进行中 · C 类✅）
#### 4.2.1 C 类 系统接入 ✅（2026-07-02）
- **测试**：`MCP 验证`（LevelPreview1-3 Play）→ 通过
  - 跑 `ScorePopupBuilder`（菜单 DPTB/Build Score UI）→ 生成 ScorePopupCanvas + 连 GameManager._scorePopup + TimerHUD
  - Player 加 CorruptionEffects 组件 + 绑 `_tracker`(CorruptionTracker) / `_volume`(Global Volume)
  - Global Volume profile = SampleSceneProfile（已含 Vignette+ColorAdjustments，4.1 误报 sharedProfile=null，编辑器模式实际有）
  - Play 验证：AddOverloadPress x5 → Value=72 → **CorruptionEffects.Level=2**（视效驱动 OK）；调 EnterWon → **ScorePopup.active=True**（评分弹窗弹）
  - 坑：`FindObjectOfType(Type)` 在 Play 模式反射不稳（NPE），改用 `FindObjectsByType<GameObject>` 遍历 + GetComponents 查组件
- **遗留**：B 类（CheckPoint 物理触发 + 按 E 打开 LoadoutUI 重绑 + 靠近 UI 提示）

#### 4.2.2 B 类 CheckPoint 多关机制（核心✅ / hintUI 待办，2026-07-02）
- **新增** `Assets/Scripts/Core/CheckPoint.cs`——碰触发→更新出生点+首次段结算 / 按 E→重绑入口 / 靠近 `_interactRange`→激活 `_hintUI`
- **改 GameManager**：加 `CheckpointReached()`（段结算→继续回 Playing，关卡连续）/ `EnterLoadoutForRebind()` / `_returnToLoadoutAfterScore` 标志 / `OnScorePopupContinue` 分流（通关→Loadout / 检查点→Playing）
- **改 FallReset**：加 `SetRespawn(pos, euler)` public
- **场景**：3 检查点挂 CheckPoint + 重命名 CheckPoint_Lv1/2/3
- **测试**：`MCP 验证` → 核心通过
  - 编译 0 错
  - Play 模拟碰 CheckPoint_Lv1（反射调 OnTriggerEnter）→ **FallReset._respawnPos 从 (0,1,-5) 更新为 (127.23,5.50,-19.27)** ✓；**GameManager.state=LevelComplete**（段结算触发）✓
- **hintUI**：3 检查点各建子 HintText（TextMesh "按 E 重绑"，默认隐藏，靠近 `_interactRange` 时 CheckPoint.Update 激活）✅
- **遗留（手动 playtest）**：按 E 重绑（需键盘实测）/ hintUI 视觉朝向（TextMesh 固定朝向，可能加 Billboard）/ 完整闭环走条（配置→玩→检查点→段结算→继续→腐败反馈）

### 4.3 能力手感优化（进行中 · 场景修复②③✅，2026-07-02）
#### 探索：关卡设计 + 场景可交互物（两个 Explore agent）
- **关卡设计文档**（`策划案/关卡设计文档.md` + `测试关卡设计标准.md`）：关 1-3 细化设计完整（双解法 + 能力数值设计值固化），关 4/5 仅骨架待白盒
- **场景可交互物**（LevelPreview1-3）：69 箱 / 15 Button / 38 Pillar_Trick / Door_front+Door_rear（关 3 双门）；可交互性全由 Tag(Pushable) + Player 端能力驱动，**无 Pushable/PullTarget 脚本组件**
- **能力数值设计值**（测试关卡标准.md）：水平跳 2m / 超载飞 4m / Dash 3m / 跳+dash 5m；余量 10%（不超 15%）；**不用 Coyote Time/Jump Buffer**；Dash 纯水平不爬高
#### 场景修复 ②③ ✅（commit 5652e19）
- ② Player 起始位置从关 2 区域 (241,13,-78) 移到**关 1 起点 (12,11,15)** 朝 +X
- ③ 25 个 Pushable 箱去 Rigidbody，统一全脚本位移（PushAbility `transform.position` 主导，避免带 Rigidbody 被物理纠正卡顿）
- ① 负 scale（31 个）：**改 playtest 驱动**——实测 FBX bake 无效（真因是场景 transform 组合 rotX=270+scale=100，非 FBX axis；之前 Explore 报告"38 Pillar_Trick 负"是推测误判，实测 Pillar_Trick 多数 lossyScale 正）
#### 关键发现：能力数值 vs 关卡设计值不对齐
- Dash `_dashDistance=5` vs 设计 3m；超载飞 0.45s×6≈2.7m vs 设计 4m
- **用户定调**：**以场景现有尺寸为准调数值**（改数值易，改场景难）→ 4.3/4.7 调参原则
#### 遗留
- 手感调参（需用户 playtest 摸问题，MCP 测不了手感）
- Button 机关系统（15 Button + 机关全无脚本，拆为 **4.10** 独立大任务）

### 4.10 关卡机关系统接入（交互物修复✅，2026-07-02，commit 98b24c7）
- **范围**：修场景已有交互物（用户定调），**不搭**文档设计但场景无几何的机关（符文门/双滑轨门/风扇归队友）
- **探索发现**（推翻之前认知）：Button 实际 10 个容器（非15）；Pillar_Trick 真正参与机关只 3 个（StepToMesa 下 y=-16）；Door_front/Door_rear 场景**不存在**（之前误判）；整个机关系统从零（无 Button.cs/Mechanism.cs 基础设施）
- **新写**：`ButtonTrigger.cs`（玩家踩/Pushable 箱压→OnPressed/OnReleased + 视觉下沉；OnPress/Toggle 模式；参考 CheckPoint/LevelExit）+ `PillarLift.cs`（协程升降，_liftHeight/_duration 参数化）
- **接入**：10 Button 挂 ButtonTrigger（Awake 设 BoxCollider isTrigger）+ 3 Pillar_Trick 挂 PillarLift + **段B 4 Button.OnPressed 绑 3 PillarLift.Lift**（任一按→3 柱全升，playtest 测推+跳）+ Pontoon×4 + Pipeline_online_canPull tag=Pushable（Push/Pull 现可作用）
- **坑**：UnityEvent public 字段必须 `= new UnityEvent()` 初始化，否则反射 GetValue 拿 null → UnityEventTools.AddPersistentListener 报 NPE
- **测试**：`MCP 验证` → 通过——Play 调段B ButtonTrigger.OnPressed.Invoke → **3 PillarLift 全部 _lifted=True + y 升起中**（-16→-14.15，目标 -1）✓
- **遗留**：
  - 段 A/C / 关3 Button 周围 8m 无机关于 → 仅视觉反馈，机关响应留 UnityEvent 接口待补几何
  - 文档设计的符文门/双滑轨门/风扇 → 场景无几何，归队友
  - 柱升高度/触发模式/4vs3 配对（已让段B 全控）→ playtest 校准

## 备注
- **4.2（多关接入）是新的最高优先**——4.1 揭示它是 demo 可玩的前提，插队在 4.3 手感/4.4 腐败之前。
- **4.9 playtest 仍最后**——验证核心乐趣，等优化 + 关 4/5 白盒就绪。
- **三个优化任务（4.3/4.4/4.7）按正交维度切**：操作反馈/系统博弈/平衡收口。
- **4.6 随机化挂起**——scope 风险最大，待设计共识。
- **关卡选择界面已移除**——走线性推进（单场景+检查点）。
- **重排历史**：2026-07-01 首次重排（删关卡选择/合并腐败/新增检查+手感+随机化/playtest 移末）；同日 4.1 完成后二次重排（新增 4.2 多关接入插队，原 4.2-4.8 顺延 4.3-4.9）。
