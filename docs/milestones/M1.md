# M1: 工程骨架与核心移动

**目标**：Unity 2022.3 LTS + URP 工程初始化、git 协作就绪；俯视 2.5D 视角下角色能在白盒测试房间里移动和跳跃。产出第一个能跑的 build——这是技术底座验证，不涉及键位重构核心玩法。

**完成标准（DoD）**：角色在白盒房间能移动（WASD）/跳跃/俯视 2.5D 相机就位；GameManager 最小框架就位（为 M2.8 状态机留接口）；构建 + MCP 验证通过。

**性质**：技术底座层。M1 不实现任何键位重构玩法（那是 M2 起）；移动/跳跃是硬编码直连，M2.1 才包装成「能力」。

## 任务清单
- [x] 1.1 初始化 Unity 工程（URP 模板）+ git init + Unity 官方 `.gitignore` + 首次提交
- [x] 1.2 项目配置：项目名、`Visible Meta Files` / `Force Text` 序列化、确认 URP 渲染管线
- [x] 1.3 新版 Input System 接入 + 基础 Action Map（Move / Look / Jump），为 M2 动态重绑留结构
- [x] 1.4 角色控制器：移动 + 跳跃 + 重力（CharacterController）+ 俯视 2.5D 相机（Cinemachine 斜俯视固定角度）+ **落点标记**（俯视跳跃纵深补偿，M1 验证可见性）
- [x] 1.5 白盒测试房间：18×18 地板 + 围墙 + 台阶/方块 + 浅沟
- [x] 1.6 GameManager / 场景管理最小框架（单例 + DontDestroyOnLoad + GameState 9 态枚举 + LoadScene 壳；不含切换，M2.8 填）

## 测试记录
- [x] 1.1 Unity 工程 + git 全部就绪；工程已入库并推送 ✅
  - 已完成（git）：`git init -b main` + Unity 官方 `.gitignore` + 初始提交 `ff7f350`；`gh auth setup-git` 配好凭证，push 走 gh token 免密
  - 已完成（Unity 工程）：Unity Hub **Universal 3D (URP)** 模板，Unity `2022.3.62f3` + URP `14.0.12`，`Assets/Settings/` 三档 URP 质量预设齐全（Performant/Balanced/HighFidelity），`SampleScene.unity` 在，首次打开无报错
  - 已完成（入库）：工程根定为 `~/projects/Unity/Projects/DontPushTheButton/`（`.git` + 文档从 cyber_sousa 合并进来）；GitHub 远程由 `cyber_sousa` 改名为 `DontPushTheButton`、remote URL 已更新；commit `c4e2e2e`（68 文件）已 push，工作区 clean
  - 验证：手动验证（Unity 首次打开无 Console 报错、git 与远程同步）→ 通过；`构建验证` `Unity -batchmode -nographics -quit` → exit 0 + "Exiting batchmode successfully" + 脚本编译无 error → 通过
  - 注意（留给 1.2）：复核 `ProjectSettings/EditorSettings.asset` 的 Visible Meta Files / Force Text（搬入沿用模板默认，1.2 确认/修正）
  - 注意（留给 1.3）：Universal 3D 模板默认**不含** Input System 包，1.3 需补装 `com.unity.inputsystem` 并切 Active Input Handling 为 Input System Package (New)/Both（可用 UnityMCP `manage_packages` 装包 + `read_console` 验证编译）

- [x] 1.2 项目配置复核（模板 + 改名时已全部设对，本轮仅确认，零改动）
  - 测试：`MCP 验证`（Unity 2022.3.62f3 内部 API 读真实运行时值）→ 通过
    - 序列化模式 `EditorSettings.serializationMode` = **ForceText** (int=2) ✅（`m_SerializationMode: 2` 字段值对应枚举 ForceText、非 Binary，已用运行时 API 证实）
    - 版本控制 `EditorSettings.externalVersionControl` = **Visible Meta Files** ✅
    - `GraphicsSettings.m_CustomRenderPipeline` GUID 7b7fd912… → `Assets/Settings/URP-HighFidelity.asset`（类型 `UniversalRenderPipelineAsset`）；`m_SRPDefaultSettings` → `Assets/UniversalRenderPipelineGlobalSettings.asset` ✅ URP 活动管线正常挂载
    - `PlayerSettings.productName` = **DontPushTheButton** ✅
  - 结论：三条协作铁律（Visible Meta Files / Force Text / URP 活动管线）均已就位，1.2 无需改动
  - 遗留：无。`Active Input Handling`（模板默认 Old）复核留待 1.3

- [x] 1.3 Input System 接入 + PlayerControls Action Map
  - 已完成（装包）：UnityMCP `manage_packages` 装 `com.unity.inputsystem 1.19.0`（URP 模板不含，已确认补装），`read_console` 0 error
  - 已完成（切 Active Input Handling）：`PlayerSettings` 无公开属性（reflection 确认），改用 `SerializedObject` 改 `ProjectSettings/ProjectSettings.asset` 的 `activeInputHandler` 字段 `0(Old) → 1(New)`，`ApplyModifiedProperties` + `SaveAssets` 落盘确认 ✅
  - 已完成（Action Map）：`Assets/Input/PlayerControls.inputactions`（JSON 格式，3 actions：Move/Look/Jump；2 控制方案：KeyboardMouse/Gamepad）+ 运行时封装 `Assets/Scripts/Input/PlayerControls.cs`（`FindAction` 暴露强类型属性）+ 一次性构建器 `Assets/Editor/InputActionAssetBuilder.cs`（菜单 `DPTB/Build Input Action Asset`）
  - 测试：`MCP 验证`（Unity 2022.3.62f3，codedom）→ 通过
    - 编译：`refresh_unity` 两脚本编译无 error（仅编译期 MCP WebSocket 短暂断连噪音，非代码问题）
    - 资产：`LoadAssetAtPath + FindAction` Move/Look/Jump 全命中；bindings Move=6(Look=2/Jump=2)；controlSchemes=[KeyboardMouse,Gamepad]
    - 文件格式：`head` 确认 JSON（`{` 开头），含 version/maps/actions/bindings/controlSchemes
  - 踩坑（已解决）：
    - `.inputactions` 必须是 **JSON**（由 InputActionImporter 导入）；`AssetDatabase.CreateAsset` 会序列化成 YAML 导致 `JSON parse error`。正确流程：`asset.ToJson()` → `File.WriteAllText` → `ImportAsset`
    - `AddControlScheme()` 返回 `ControlSchemeSyntax` builder，配套方法是 `WithRequiredDevice`（非 `WithDevice`，后者作用于已构建的 `InputControlScheme`）
  - 遗留：无（原"New 需重启生效"项已关闭）
  - 重启验证（2026-06-17 重启 Unity 后补 `MCP 验证`）：`activeInputHandler=1(New)` 运行时确认；**InputSystem 运行时已激活**——`InputSystem.devices` 枚举出真实硬件 `FastKeyboard(Keyboard)` / `FastMouse(Mouse)`（New 模式生效的决定性证据，Old 模式下不枚举设备）；资产仍有效（3 actions）→ 通过
  - 注意（留给 1.4）：`PlayerControls.cs` 已 `[RequireComponent(typeof(CharacterController))]` 预留；1.4 把 `InputActionAsset` 拖到 PlayerControls 的 `_actions` 字段 + 实现 `PlayerMover` 读取 Move/Jump 驱动 CharacterController，俯视 2.5D 相机（斜俯视固定角度）另挂

- [x] 1.4 角色控制器（CharacterController）+ 俯视 2.5D 相机 + 落点标记
  - 已完成（数值配置）：`Assets/Scripts/Config/MovementTuning.cs`（ScriptableObject，moveSpeed=5/jumpHeight=1.5/gravity=-20/turnSpeed=720 占位值）+ `Assets/Config/MovementTuning.asset`，满足「数值全部走 SO 集中配置」跨里程碑约束
  - 已完成（角色控制器）：`Assets/Scripts/Player/PlayerMover.cs`——读 PlayerControls 的 Move/Jump，`CharacterController.Move` 驱动移动/跳跃/重力；水平方向相对相机水平面变换；面向移动方向平滑旋转；贴地速度钳制避免下落累积。`[RequireComponent(CharacterController)]` + `[RequireComponent(PlayerControls)]`
  - 已完成（落点标记）：`Assets/Scripts/Player/LandingMarker.cs`——扁平黄色圆盘（Cylinder 压扁）钉在角色 XZ 地面投影、Y 固定地面，LateUpdate 跟随；跳跃时角色浮起、圆盘留落点（俯视纵深补偿，GDD 锁定需求）
  - 已完成（俯视相机）：装 **Cinemachine 3.1.7**（2022.3 取到的是 3.x 非 2.x；命名空间 `Unity.Cinemachine`，主类 `CinemachineCamera`）；`Assets/Editor/CinemachineTopDownBuilder.cs`（菜单 `DPTB/Build Top-Down Camera`）配出 `CM TopDown`：Follow=Player、Aim=不挂(旋转固定)、Body=CinemachineFramingTransposer(硬跟随/无死区/无阻尼)、俯角 50°/距离 14；Main Camera 挂 CinemachineBrain
  - 已完成（场景）：`Assets/Scenes/SampleScene.unity`——Player(Capsule+CharacterController+PlayerControls+PlayerMover，`_actions`/`_tuning`/`_relativeCamera` 已赋值) + Floor(Cube 20×1×20 静态+BoxCollider，**临时地板，1.5 替换为白盒房间**) + LandingMarker(黄色圆盘) + CM TopDown 相机；build settings 已指向规范路径
  - 测试：`MCP 验证`（Unity 2022.3.62f3 编辑器交互）→ 通过
    - 编译：4 个新脚本 `read_console` 0 error
    - 运行时：`manage_editor play` 进 Play mode，`read_console` 0 error / 0 warning（PlayerControls._actions 引用正确，无「InputActionAsset 未赋值」报错）
    - 装配：`get_hierarchy` rootCount=7 全到位；`get component` 复核 `_actions`=PlayerControls.inputactions / `_tuning`=MovementTuning.asset / `_relativeCamera`=Main Camera / LandingMarker.`_target`=Player 全部正确解析
    - 视觉：`manage_camera screenshot`(game_view) + 图像分析 → 俯视 2.5D 正常（~50° 斜俯视有立体感）、胶囊角色立于地面、**黄色落点圆盘在脚下**、实时阴影生效、材质正常无粉色
  - 遗留：**移动/跳跃手感未自动化验证**（MCP 难模拟键盘输入）——WASD 移动响应 / 空格起跳高度 / 转向手感留人工试玩；数值（moveSpeed/jumpHeight/gravity）为占位，M4 playtest 调参
  - 踩坑（已解决）：
    - Cinemachine 在 2022.3 装到 **3.1.7**（3.x），API 与 2.x 完全不同——用 `unity_reflect` 反射核实 `Unity.Cinemachine.CinemachineCamera`/`CinemachineFramingTransposer` 实际成员再写 builder，避免凭记忆写 2.x API
    - `manage_material create` 把 `material_path="Assets/Materials"` 当文件名建出 `Assets/Materials.mat`（Standard shader，URP 项目会粉色）——改用 `execute_code` 建 `Assets/Materials/LandingMarker_Mat.mat`(URP/Lit) + `manage_asset delete` 清理误建文件
    - `manage_scene save` 的 `scene_path` 参数无效、仍按 name 存到 `Assets/` 根——用 `execute_code`(EditorSceneManager.SaveScene SaveAs + 删根副本 + EditorBuildSettings.scenes) 规范到 `Assets/Scenes/`

- [x] 1.5 白盒测试房间（18×18：地板 + 围墙 + 台阶/方块 + 浅沟）
  - 已完成（房间）：`Assets/Scenes/SampleScene.unity` 扩为正式白盒——前/后两块地板（18×8，中央留 2m 浅沟）+ 沟底（深 1m，掉进可跳出）+ 4 围墙（高 4m，> 跳跃顶点防跳出）+ 0.6m 台阶（>stepOffset 0.3，验证跳上）+ 1m 方块（跳越/跳上）+ 浅沟（跳过 + 落点纵深，呼应 GDD 俯视补偿）；统一 `Assets/Materials/Whitebox_Mat.mat`(URP/Lit 浅灰)；删除 1.4 临时 Floor；Player 起点移前板 (0,1,-5)
  - 布局：前板(z[-9,-1])=Player(z-5)+方块(z-3) → 浅沟(z[-1,1],深1m) → 后板(z[1,9])=台阶(z3)；助跑跳沟到后板
  - 测试：`MCP 验证` → 通过
    - 运行时：`manage_editor play` 进 Play mode，`read_console` 0 error / 0 warning
    - 视觉：`manage_camera screenshot`(game_view) + 图像分析 → 18×18 房间成型：前后地板 + 横向浅沟、4 围墙、台阶、方块、Player + 黄色圆盘在脚下，材质正常无粉色
  - 遗留：**碰撞/跳跃手感未自动化**（围墙阻挡 / 跳台阶 / 跳沟 / 墙角不卡）→ 已入 `docs/tests/M1-手动验证清单.md` 第 2 节（含浅沟 2.6），留 M1 全完成一次性测

- [x] 1.6 GameManager 框架（单例 + GameState 枚举 + 场景加载壳，不含切换）
  - 已完成：`Assets/Scripts/Core/GameState.cs`（9 态枚举：Boot/MainMenu/Onboarding/Loadout/Playing/Paused/LevelComplete/Failed/GameComplete，覆盖 GDD 全流程）+ `Assets/Scripts/Core/GameManager.cs`（MonoBehaviour 单例 + 重复守护 + `DontDestroyOnLoad` 跨场景持久化 + `CurrentState`(初始 Boot) + `LoadScene(string)` 壳接口；**不实现状态切换**，留 `TODO(M2.8)`）；场景加 GameManager GameObject
  - 测试：`MCP 验证` → 通过
    - 编译：2 脚本 `read_console` 0 error
    - 运行时：`manage_editor play` + `execute_code` 查 `GameManager.Instance`=GameManager（非 null，单例建立）/ `CurrentState`=Boot（初始，无切换）/ `gameObject.scene.name`=DontDestroyOnLoad（持久化生效）；`read_console` 0 error / 0 warning（无重复单例警告，守护正常）
  - 遗留：**状态切换逻辑**（转换合法性校验、各状态副作用）→ M2.8；手动项（单例唯一/枚举完整/不自动切换）已在清单第 3 节

## ✅ M1 全部完成（2026-06-19，6/6 子任务）
自动化 / MCP 验证已全过（编译 / Play / 装配 / 视觉）。**唯一待办：整里程碑手动测试**——按 `docs/tests/M1-手动验证清单.md` 一次性测移动跳跃手感、白盒碰撞、落点纵深、GameManager 框架；通过后开 M2 分支。

## 备注
- **验证手段升级（基础设施就绪，2026-06-17）**：已接入 **MCP for Unity**（CoplayDev/unity-mcp 包 v9.7.3 / server v3.4.2，本地 clone 于 `/home/awaken/projects/Unity/unity-mcp`）。Unity 编辑器打开 + MCP 面板 session active 时，Claude 经 `UnityMCP`（`http://127.0.0.1:9090/mcp`，local scope 不进 git）可直接操作 Unity。实测通过：`instances`→DontPushTheButton@2022.3.62f3、`editor_state.ready_for_tools=true`、`read_console`/`manage_scene get_active`(SampleScene)/`find_gameobjects Main Camera` 均正常。端口 9090（8080 被 SRS 流媒体服务占用）。与 `-batchmode` 互补——用 MCP 验证时测试记录标注 `MCP 验证`。
- **视角：俯视 2.5D（已锁定 2026-06-19）**——3D 场景 + 斜俯视固定角度相机。物理交互（推箱/拉物/磁力枪）方向清晰；跳跃纵深（沟/裂隙）靠角色阴影 + 边缘视觉标记补偿（关卡设计需配合）。
- **角色用 CharacterController**（解谜 + 物理交互更可控）；若 M2 推动箱子需要真实物理再评估是否切 Rigidbody。
- M1 的移动/跳跃是硬编码直连；M2.1 会把它重构包装成「能力(Ability)」，此处不提前抽象。
- Unity 工程文件不要手工编辑 meta，让 Editor 生成。
- **设计 SoT 就绪（2026-06-19）**：GDD v1.0（整合队友 v0.9 + 我们的 v0.9a）+ milestone M1-M4 重排 + `docs/toConfirm.md` 决策记录，已合并 main（PR#2）。视角锁定俯视 2.5D。**M1.4 完成**（Cinemachine 3.x 俯视相机 + CharacterController 移动/跳跃/重力 + 黄色落点圆盘；MCP 验证编译/Play/视觉全过，手感留人工试玩）。**下一步 M1.5**：白盒测试房间（替换临时地板 + 围墙 + 可跳台阶/障碍）。当前在 `feature/m1-character-controller` 分支。
