# M3 手动验证清单

> M3 任务的手动 / playtest 验证项（手感、人工观察类）。按 milestone-rules，手动项不阻塞提交，留里程碑收尾一次性测。
> 当前实现参数见 `docs/config/微调参数表.md`。

---

## 3.1 拾取能力（磁力枪·抓取搬运 / Pickup）

> Play mode，键盘。MCP 已验证核心状态机（抓取进 _carried / 吸附前方 carryDistance=1.5 / 释放清空）。配置：Pickup 绑 E（普通）或超载键。

- [x] **3.1a 抓取与跟随**
  - 操作：走到 TestCrate 旁（< grabRange=2），按 E
  - 期望：箱子被吸附到玩家前方 carryDistance 处，随玩家移动/转向跟随
- [x] **3.1b 释放**
  - 操作：持有状态下再按 E
  - 期望：箱子放下（停在当前位置），不再跟随
- [x] **3.1c 抓取切换稳定**
  - 操作：释放后再按 E（面对箱子），反复切换
  - 期望：Idle↔Carrying 切换稳定，无卡死/重抓异常
- [x] **3.1d 超载搬多个**
  - 操作：Pickup 绑超载键，多个 Pushable 在 grabRange 内，按超载键
  - 期望：吸附范围内全部（上限 maxCarryCount=3），横向排列跟随；腐败 +IncrementPerPress 一次
- [x] **3.1e 边界（抓取失败不扣腐败）**
  - 操作：grabRange 外按 E / 无 Pushable 时按超载键
  - 期望：不抓取（保持 Idle）；超载键也不扣腐败（抓取失败不 ChargeOverload）

---

## 3.2 拉动能力（磁力枪·牵引 + 超载抓钩）

> Play mode，键盘。MCP 已验证（普通拉吸到前方 pullToDistance=1.5 / 超载抓钩玩家位移到命中点前方 hookLandDistance）。配置：Pull 绑普通键（拉）或超载键（抓钩）。

- [x] **3.2a 普通拉**
  - 操作：面对 TestCrate（< grabRange=2），按拉动键（普通键）
  - 期望：箱子吸到玩家前方 pullToDistance 处
- [x] **3.2b 超载抓钩**
  - 操作：Pull 绑超载键，远处（< hookRange=8）面对 Pushable，按超载键
  - 期望：玩家瞬时位移到目标前方 hookLandDistance；腐败 +IncrementPerPress 一次
- [x] **3.2c 边界（无目标不扣腐败）**
  - 操作：grabRange 外按普通拉 / 无 Pushable 时按超载抓钩
  - 期望：普通拉不吸（无目标）；抓钩不触发也不扣腐败（射线未命中 Pushable）
- [x] **3.2d 纵深补偿**（俯视 2.5D）
  - 操作：抓钩位移到不同纵深目标
  - 期望：落点安全（不掉沟）；纵深靠阴影/边缘标记补偿（v1.0「调试可接受」）

---

## 3.4 冲刺能力（dash + 超载 dash×2）

> Play mode，键盘。MCP 已验证（普通 dash=dashDistance=5 / 超载 dash×2=10）。配置：Dash 绑普通键或超载键。

- [x] **3.4a 普通 dash**
  - 操作：按移动方向（如 W 或 D）+ dash 键
  - 期望：朝角色正前方瞬时突进 dashDistance（不管按 W/D/A/S，只要在移动都朝面朝方向冲）；无移动输入不 dash
- [x] **3.4b 超载 dash×2**
  - 操作：Dash 绑超载键，按超载键
  - 期望：突进距离×2；腐败 +IncrementPerPress 一次（J4：1 次触发算 1 次）
- [x] **3.4c 防穿墙**
  - 操作：面对墙 dash
  - 期望：停在墙前 0.1m（不穿墙）
- [x] **3.4d 冷却**
  - 操作：连按 dash
  - 期望：冷却内不触发（cooldown=0.5s）

---

## 3.3 推动光束型（Push / 原 PushBeam）

> Play mode，键盘。MCP 已验证（射线推 TestCrate beamPushDistance=3）。配置：关卡 offer Push（PushAbility，光束型）时，Push 绑键。

- [ ] **3.3a 普通光束推**
  - 操作：面对 Pushable（< beamRange=8），按 Push 键
  - 期望：物体被推 beamPushDistance（朝玩家朝向）
- [ ] **3.3b 超载光束推**
  - 操作：Push 绑超载键，按超载键
  - 期望：推距×2；腐败 +IncrementPerPress 一次
- [ ] **3.3c 二选一（关卡固定）**
  - 操作：关卡 LevelConfigSO offer Pickup（拾取搬运）或 Push（光束推）
  - 期望：该关玩家只能用对应 variant（配置 UI 只显 offer 的）

---

## 3.5 跳跃超载飞行状态机（+ 缓落）

> Play mode，键盘。MCP 已验证（状态机 IsSlowFalling + ApplyGravity 缓落 FallGravityScale=0.5）。配置：Jump 绑超载键触发飞行。

- [x] **3.5a 超载飞行**
  - 操作：Jump 绑超载键，按超载键起跳
  - 期望：初速×倍率 + 飞行期持续上升（OverloadFlyDuration）；腐败 +IncrementPerPress 一次
- [x] **3.5b 缓落**
  - 操作：超载飞行结束后观察下落
  - 期望：下落速度减半（FallGravityScale=0.5，缓落感）；落地恢复正常
- [x] **3.5c 普通跳**
  - 操作：Jump 绑普通键，按起跳
  - 期望：√(2gh) 正常跳，正常重力落（不缓落）
- [x] **3.5d 飞行时长可调**
  - 操作：改 JumpTuning._overloadFlyDuration，playtest
  - 期望：飞行时长随之变（SO 调参）

---

## 3.10 代码动画（队友实现：轮子/螺旋桨/车身）

> 队友基于状态接口（`docs/design/动画系统.md`）用代码控制部件 Transform 实现机器人动画。代码侧单测过（AbilityStateContractTests 保接口契约）。以下为队友交付后的 playtest。

- [ ] **3.10a 轮子随速度转**
  - 操作：WASD 移动（快/慢/停）
  - 期望：轮子转速随 MoveInput.magnitude；停下不转或缓停
- [ ] **3.10b 螺旋桨**
  - 操作：常驻观察 + 触发超载飞行
  - 期望：螺旋桨常转；IsFlying 时加速
- [ ] **3.10c 车身姿态**
  - 操作：移动 / 起跳下落 / Dash
  - 期望：移动后仰、上升上仰、Dash 前倾（OnInstantCast）
- [ ] **3.10d 动画不干扰物理**
  - 操作：移动转向 + 触发各能力
  - 期望：只改部件 localTransform，Player 根 position/rotation 不受动画影响（朝向仍跟移动方向）

---

## 3.7 评分系统（4 星 + 软计时 + 结算弹窗）

> Play mode。代码侧单测过（ScoreCalculatorTests，26/26）。以下为完整过关流程 playtest。

- [ ] **3.7a 结算弹窗**
  - 操作：配置 → 开始 → 走到 LevelExit 过关
  - 期望：ScorePopup 弹出（4 星图标 + 用时 + 超载次数 + 空槽数 + X/4 + 继续按钮）；通关星恒亮
- [ ] **3.7b 计时 HUD**
  - 操作：关卡中观察
  - 期望：TimerHUD 显示用时（mm:ss），进关开始走，过关停
- [ ] **3.7c 4 星判定组合**
  - 操作：分别「快速+无超载+少绑」（4 星）/「慢+多超载+绑满」（1 星通关）
  - 期望：时间/节制/精简星按阈值（30s/3次/1空槽）亮灭
- [ ] **3.7d 继续回配置**
  - 操作：弹窗点继续
  - 期望：回配置 UI；重玩星级取最高（PlayerPrefs）

---

## 3.8 腐败 URP 视效（层级泛红/glitch + 机制层）

> Play mode。代码侧单测过（CorruptionEffectsTests，28/28）。以下为超载腐败 playtest。

- [ ] **3.8a 泛红渐强**
  - 操作：连续按超载键（腐败涨，跨过 25%/50%/75%/95% 阈值）
  - 期望：屏幕边缘泛红（Vignette）随层级渐强；L3+ 色偏 glitch 感
- [ ] **3.8b 移速降（L2+）**
  - 操作：腐败到 50%+ 后移动
  - 期望：移速 ×0.85（变慢）
- [ ] **3.8c 冷却延长（L3+）**
  - 操作：腐败到 75%+ 后连按 Dash/Push
  - 期望：冷却 ×1.3（变长）
- [ ] **3.8d 满格重置**
  - 操作：腐败到 100%
  - 期望：EnterFailed 重置（M2.6 已有）+ 腐败清零

---

## 3.11 集成联调（关 1/关 2 完整流程 + 完整主角动画）

> Play mode。代码侧 MCP 已验证（GameManager 教学关路径、主角自建 WASD Loadout、Play 0 异常）。完整主角=代码+骨骼Rig+动画驱动（ProtagonistRigAnimator 消费 M3.10 接口）。

### 关 1（Level_01·教学关·纯移动）

> Unity 打开 `Assets/Scenes/Levels/Level_01.unity` → Play。关 1 跳过 Loadout 直接 Playing，WASD 移动。

- [ ] **3.11a 主角显示**：完整机器人骨骼模型（非默认立方体），摄像机能看到
- [ ] **3.11b WASD 移动**：主角响应；方向基于 Main Camera（⚠️ Level_01 用 Main Camera 非 CM TopDown，角度可能非正俯视）
- [ ] **3.11c 走到出口**：从起点（x≈-20 着陆舱）沿白盒走到终点 Platform（x≈329）的 LevelExit
- [ ] **3.11d 过关触发**：进 LevelExit trigger → 评分弹窗；关 1 纯移动无超载，应近 4 星
- [ ] **3.11e 重玩**：弹窗点继续 → 可重玩（教学关路径）

### 关 2（SampleScene·双解法 + 评分 + 腐败）

> Unity 打开 `SampleScene` → Play。走 Loadout 配置 → Playing。

- [ ] **3.11f 配置绑键**：Loadout UI 绑移动 + 至少一个能力到超载键；开始按钮可点
- [ ] **3.11g 双解法过裂口**：超载跳/推/拉 都能过关 2 裂口
- [ ] **3.11h 评分弹窗**：过关 → 4 星计算（通关/时间/节制/精简）
- [ ] **3.11i 腐败视效**：连续超载 → 泛红渐强 L1→L4 + L2 移速降 + L3 冷却延长（见 3.8）

### 完整主角动画（关 1/关 2 都观察）

- [ ] **3.11j 轮子转动**：移动时车轮旋转（ProtagonistUnderpan）
- [ ] **3.11k 车身转向**：转向时车体平滑转向（_turnSpeed=450）
- [ ] **3.11l 惯性摇晃**：加减速/转向时车身惯性倾斜（InertanceShake；⚠️ SO 惯性参数队友留 0，可能不明显，待调）
- [ ] **3.11m 螺旋桨（飞行）**：关 2 超载跳 IsFlying → 螺旋桨加速（_screwOverloadSpeed=2000）
- [ ] **3.11n 引擎喷口（dash）**：关 2 dash → 引擎喷口瞬时转（_ventMaxSpeed=1500）

> ⚠️ 遗留：Level_01 用 Main Camera（非 CM TopDown 俯视），视角/移动方向可能非正俯视；如要正俯视 2.5D，后续换 CM TopDown。
