# 微调参数表 (Tuning Parameters)

> **单一事实来源**：记录所有「后期可微调」的参数（手感 / 数值 / 阈值），供调参与 playtest 对照。
>
> **维护规则**：每完成一个 milestone 小节（`M{N}.x`），若新增或修改了可调参数，**必须同步更新本表**（见 `.claude/rules/milestone-rules.md`）。参数化规范见 `.claude/rules/code-conventions.md`。
>
> **调参方式速查**：
> - **SO 字段** → 改 `.asset`（Play 中即时生效，重启 Play 更稳）
> - **组件 `[SerializeField]`** → 改 Inspector
> - **场景对象属性**（如 `CM TopDown`）→ 改场景 Inspector
> - **editor builder const** → 改 const + 重跑菜单（一般无需，运行时调场景对象即可）

---

## M1 · 工程骨架与核心移动

### 移动 / 跳跃 — `MovementTuning` SO
> 资产：`Assets/Config/MovementTuning.asset` · 来源：M1.4

| 字段 | 当前值 | 单位 | 调什么手感 |
|------|--------|------|-----------|
| `_moveSpeed` | 5 | m/s | 水平移动快慢 |
| `_jumpHeight` | 1.5 | m | 跳跃高度（反推起跳速度） |
| `_gravity` | -20 | m/s² | 下落快慢（负 = 向下） |
| `_turnSpeed` | 720 | °/s | 转向速度（`<=0` = 瞬切朝向） |
| `_groundStickVelocity` | -2 | m/s | 贴地钳制（grounded 时下落速度上限，避免无限累积） |

### PlayerMover 组件 `[SerializeField]`
> 挂在：Player · 来源：M1.4

| 字段 | 当前值 | 说明 |
|------|--------|------|
| `_moveInputThreshold` | 0.01 | 输入死区：水平输入向量平方长度低于此值视为静止（不移动/不转向） |
| `_tuning` | MovementTuning.asset | 数值引用 |
| `_relativeCamera` | Main Camera | 水平移动方向所相对的相机 |

### 落点标记 — `LandingMarker` 组件
> 挂在：LandingMarker（黄色圆盘） · 来源：M1.4

| 字段 | 当前值 | 说明 |
|------|--------|------|
| `_groundY` | 0.02 | 圆盘吸附的地面 Y 高度 |
| `_target` | Player | 跟随目标（角色） |

### 俯视相机 — Cinemachine
> 场景对象：`CM TopDown`（运行时调，最常用） · builder：`Assets/Editor/CinemachineTopDownBuilder.cs`（const，改初始默认值需重跑菜单 `DPTB/Build Top-Down Camera`） · 来源：M1.4

| 参数 | 当前值 | 位置 | 调什么 |
|------|--------|------|--------|
| 俯角 Pitch | 50° | CM TopDown Transform.rotation.x / builder const | 相机俯视角度 |
| 偏航 Yaw | 0° | 同上 | 相机水平偏转 |
| 距离 Distance | 14 | FramingTransposer.m_CameraDistance / builder const | 相机距角色远近 |
| 死区 W/H/Depth | 0 | FramingTransposer.m_DeadZoneWidth/Height/Depth | 跟随死区（0 = 硬跟随） |
| 阻尼 X/Y/Z | 0 | FramingTransposer.m_XDamping 等 | 跟随平滑度（0 = 无） |

### CharacterController（Unity 内置，Inspector 调）
> 挂在：Player · 来源：M1.4

| 属性 | 当前值 | 调什么 |
|------|--------|--------|
| radius | 0.5 | 碰撞胶囊半径 |
| height | 2 | 胶囊高度 |
| stepOffset | 0.3 | 自动踏上低台阶的高度（< 此值可直接走上去） |
| slopeLimit | 45° | 可行走坡度 |

---

## M2.1 · 能力系统骨架（移动 / 跳跃 / 推动）

### 推动能力 — `PushTuning` SO
> 资产：`Assets/Config/PushTuning.asset` · 来源：M2.1
> ⚠️ PushAbility 已被 **M3.1 升级为磁力枪抓取搬运型**（Trigger Continuous，Idle↔Carrying）。下方 M2.1 射线推字段已退役留作历史；当前搬运字段见 **M3.1 段**。

| 字段 | 当前值 | 单位 | 调什么 |
|------|--------|------|--------|
| `_range` | 2 | m | 推动作用距离（按 E 时前方此距离内命中才推） |
| `_pushDistance` | 1.5 | m | 单次推动位移 |
| `_cooldown` | 0 | s | 推动冷却（`<=0` = 无；M3 调参） |

### 能力组件 `[SerializeField]`
> 挂在：Player · 来源：M2.1

| 组件 | 字段 | 当前值 | 说明 |
|------|------|--------|------|
| MoveAbility | `_moveInputThreshold` | 0.01 | 输入死区（从 PlayerMover 迁移，值不变） |
| PushAbility | `_pushTuning` | PushTuning.asset | 推动调参引用 |
| PushAbility | `_rayHeight` | 0.5 | m | 射线发射世界 Y 高度（对准物体中心；1m 箱子中心约 0.5） |
| PlayerAbilityController | `_tuning` | MovementTuning.asset | 运动调参引用（从 PlayerMover 迁移） |
| PlayerAbilityController | `_relativeCamera` | Main Camera | 水平移动相对相机（从 PlayerMover 迁移） |

> 注：`PlayerMover` 已于 M2.1 退役（移动/跳跃/重力逻辑迁入 `PlayerAbilityController` + `MoveAbility`/`JumpAbility`，新增 `PushAbility`）。M1 段 PlayerMover 参数见上方，仅供历史对照。

---

## M2.3 · 关卡配置 ScriptableObject

> ⚠️ **关卡配置数据（每关一个 `LevelConfigSO.asset`），非全局手感调参**。此处登记字段供查阅；改关卡内容 = 改对应 `.asset`，而非全局数值。

### `LevelConfigSO` 字段
> 资产：`Assets/Config/LevelX.asset`（如 Level2.asset）· 来源：M2.3

| 字段 | 关2 值 | 说明 |
|------|--------|------|
| `_levelId` | Level2 | 关卡唯一标识 |
| `_displayName` | 裂口 | 显示名 |
| `_sceneName` | (空，M2.7 填) | 关卡场景名 |
| `_slotCount` | 6 | 本关槽位数（GDD 4.5） |
| `_availableKeys` | W/A/S/D/Space(超载)/E | 可用按键 + 超载身份标记（KeySlotDef.IsOverload，O2） |
| `_availableAbilities` | Jump, Push | 可绑能力（不含移动；移动方向 Up/Down/Left/Right 默认每关可用，GDD §A） |

---

## M2.5 · 运行时重绑 + 超载系统

> 超载强化参数本期 `[SerializeField]` 写死（H3 划界），M3.5 走 SO 调参。

### 超载强化（各能力 `[SerializeField]`）
> 挂在：Player 的能力组件 · 来源：M2.5

| 组件 | 字段 | 当前值 | 说明 |
|------|------|--------|------|
| MoveAbility | `_overloadSpeedMultiplier` | 1.8 | 超载移动速度倍率（StatStack） |
| JumpAbility | `_overloadJumpMultiplier` | 1.4 | 超载跳跃初速倍率 |
| JumpAbility | `_overloadFlyDuration` | 0.45 s | 超载飞行持续（StateSwitch 短飞） |
| JumpAbility | `_overloadFlyVelocity` | 6 m/s | 飞行期上升速度 |
| PushAbility | `_overloadPushMultiplier` | 2 | 超载推距倍率（StatStack） |

### 腐败 — `CorruptionTracker`
> 挂在：Player · 来源：M2.5（最小版；M2.6 加 HUD 腐败条 + 数据 SO + 永不恢复强化）

| 字段 | 当前值 | 说明 |
|------|--------|------|
| `_maxValue` | 100 | 满格阈值（GDD 4.4） |
| `_incrementPerPress` | 12 | 每次按超载键的腐败增量（GDD [PLACEHOLDER]，M3 调参） |

---

## M2.6 · 腐败系统

### `CorruptionTuning` SO
> 资产：`Assets/Config/CorruptionTuning.asset` · 来源：M2.6

| 字段 | 当前值 | 说明 |
|------|--------|------|
| `_maxValue` | 100 | 满格阈值（GDD 4.4，满格→重置） |
| `_incrementPerPress` | 12 | 每次按超载键的腐败增量（GDD [PLACEHOLDER]，M3 调参） |

> CorruptionTracker 引用 SO；永不恢复（本关只增）；满格事件 `OnCorruptionFull` 预留 M2.8 接状态切换。

---

## M2.7 · 示范关卡 关2裂口（布局数值）

> 白盒关2布局（SampleScene）。playtest 调（M2.9）。

### 关2几何
| 对象 | 位置(pos) | 尺寸(scale) | 说明 |
|------|----------|------------|------|
| FrontFloor | (0,-0.5,-5.25) | (18,1,6) | 起点平台（顶面 y=0，z=-8.25~-2.25） |
| BackFloor | (0,-0.5,5.25) | (18,1,6) | 对岸平台（z=2.25~8.25） |
| Pit | (0,-8,0) | (18,1,5) | 沟底（深，FallReset y<-5 先触发） |
| 沟（空隙） | z=-2.25~2.25 | — | **宽 4.5m**（普通跳 3.87 过不去） |
| TestCrate | (0,0.5,-3.5) | (1,1,1) | 关2箱子（Pushable），推到 z=0 做中部踏板 |
| LevelExit | (0,1,6.5) | (4,2,1) | 出口触发器（绿色 cube，tag Player 触发） |

### FallReset（Player 上）
| 字段 | 值 | 说明 |
|------|-----|------|
| `_fallThreshold` | -5 | 掉沟 y 阈值 |
| `_respawnPos` | (0,1,-5) | 重置起点 |
| `_respawnEuler` | (0,0,0) | 朝 +z（沟方向） |

> 解法：A) 推箱到 z=0 踏板 → 两段跳每段 2.25m（<3.87）；B) 超载跳 ≈4.85m（>4.5）。

---

## M3.1 · 推动完整系统（磁力枪·抓取搬运型）

> PushAbility 从 M2.1 瞬时射线推升级为**持续抓取搬运状态机**（Idle↔Carrying，切换式按 E 抓/放）。资产：`Assets/Config/PushTuning.asset`。

### PushTuning SO（M3.1 搬运字段）
| 字段 | 当前值 | 单位 | 调什么 |
|------|--------|------|--------|
| `_grabRange` | 2 | m | 抓取检测距离（按 E 时此距离内 Pushable 可被抓取） |
| `_carryDistance` | 1.5 | m | 持有物体吸附在玩家前方的距离 |
| `_carrySpacing` | 1 | m | 多物体搬运时横向间距 |
| `_maxCarryCount` | 3 | 个 | 超载最多同时搬运数量 |

> `_range`/`_pushDistance`/`_cooldown`（M2.1 旧字段）保留给 3.3 牵引光束型，本期不用。

### PushAbility 组件变化（Player 上）
| 项 | M2.1（旧） | M3.1（现） |
|----|-----------|-----------|
| Trigger | Instant | **Continuous**（持有需每帧跟随） |
| Overload | StatStack（推距×2） | **MultiTarget**（搬多个） |
| `_grabHeight`（原 `_rayHeight`） | 0.5 | 0.5（抓取/持有物体 Y 高度） |
| 交互 | 按 E 瞬时推一下 | 按 E 抓取↔释放（切换式） |
| 超载 | 推距×2 | 抓取范围内上限 `_maxCarryCount` 个，成功 ChargeOverload 扣一次 |

---

## M3.2 · 拉动能力（磁力枪·牵引 + 超载抓钩）

> 新增 PullAbility（Kind=Pull，Instant）。普通：前方 grabRange 内最近 Pushable 吸向自己；超载（v1.0 抓钩）：射线命中 Pushable → 玩家瞬时位移到目标前方。资产：`Assets/Config/PullTuning.asset`。复用 Pushable tag。

### PullTuning SO
| 字段 | 当前值 | 单位 | 调什么 |
|------|--------|------|--------|
| `_grabRange` | 2 | m | 普通拉检测距离（前方此距离内最近 Pushable 被吸向自己） |
| `_pullToDistance` | 1.5 | m | 目标吸到玩家前方的距离 |
| `_hookRange` | 8 | m | 超载抓钩射线距离（命中 Pushable 作为落点） |
| `_hookLandDistance` | 1.5 | m | 玩家落到命中点前方的距离 |

### PullAbility 组件（Player 上）
| 字段/项 | 值 | 说明 |
|---------|-----|------|
| `_pullTuning` | PullTuning.asset | 拉动调参引用 |
| `_grabHeight` | 0.5 | 检测/射线 Y 高度 |
| Trigger / Overload | Instant / StateSwitch | 超载=自身位移（抓钩） |
| 普通 | — | 前方 grabRange 最近 Pushable → 吸到玩家前方 pullToDistance |
| 超载 | — | RaycastAll（跳过自身）命中 Pushable → Teleport 玩家到命中点前方 hookLandDistance（CharacterController disable/set/enable） |

---

## M3.4 · 冲刺能力（dash + 超载 dash×2）

> 新增 DashAbility（Kind=Dash，Instant）。关 4 引入。普通：向移动输入方向（无输入按朝向）瞬时突进；超载（J4）：距离×2，1 次触发算 1 次腐败。资产：`Assets/Config/DashTuning.asset`。

### DashTuning SO
| 字段 | 当前值 | 单位 | 调什么 |
|------|--------|------|--------|
| `_dashDistance` | 5 | m | 单次冲刺位移（超载 ×2 = 10） |
| `_cooldown` | 0.5 | s | 冷却（防连按滥用） |

### DashAbility 组件（Player 上）
| 字段/项 | 值 | 说明 |
|---------|-----|------|
| `_dashTuning` | DashTuning.asset | 冲刺调参引用 |
| `_dashHeight` | 0.5 | Raycast 防穿墙检测 Y |
| Trigger / Overload | Instant / StatStack | 超载=距离×2 |
| 方向 | — | 移动输入方向（无输入按朝向） |
| 防穿墙 | — | RaycastAll 最近非玩家障碍，墙前 0.1m 停（Teleport） |

---

## 变更记录

| 日期 | milestone | 改动 |
|------|-----------|------|
| 2026-06-19 | M1.4 | 初始参数：MovementTuning(moveSpeed/jumpHeight/gravity/turnSpeed) + 相机(Pitch/Yaw/Distance) + CharacterController + LandingMarker |
| 2026-06-19 | M1.4 修复 | + MovementTuning.`_groundStickVelocity`（起跳 bug 修复时新增） |
| 2026-06-19 | M1.4 清理 | + PlayerMover.`_moveInputThreshold`（参数化清理，原 0.01f magic number） |
| 2026-06-20 | M2.1 | + PushTuning SO(`_range`/`_pushDistance`/`_cooldown`) + PushAbility(`_rayHeight`)；`_moveInputThreshold`/`_tuning`/`_relativeCamera` 从 PlayerMover 迁移至 MoveAbility/PlayerAbilityController；PlayerMover 退役 |
| 2026-06-20 | M2.3 | + LevelConfigSO（关卡配置数据：槽位/可用按键+超载/可用能力；非全局手感调参） |
| 2026-06-20 | M2.5 | + 超载强化倍率(移动1.8/跳跃1.4/推2)+飞行(0.45s/6m/s)+CorruptionTracker(max100/press12)；[SerializeField] 写死，M3.5 走 SO |
| 2026-06-20 | M2.6 | + CorruptionTuning SO(maxValue/increment)，CorruptionTracker 改引用 SO + OverloadCount + OnChanged；+ CorruptionHUD（腐败条+计数） |
| 2026-06-20 | M2.7 | + 关2布局（沟宽4.5/平台/箱位/出口）+ LevelExit 触发器 + FallReset（阈值-5/起点0,1,-5）；playtest 调 |
| 2026-06-20 | M2.9 | playtest 修 builder bug 链（UISetAnchored→UIPlace / Text 黑字 / Instantiate SetActive / _slotUIs Clear / GetComponent 引用 / GameManager 自愈 / CorruptionHUD Lerp）；默认配置绑能力 |
| 2026-06-20 | M3.1 | PushAbility 瞬时射线推→Continuous 抓取搬运状态机（Idle↔Carrying，切换式按 E 抓/放）；PushTuning +`_grabRange`/`_carryDistance`/`_carrySpacing`/`_maxCarryCount`（搬运），旧 range/pushDistance/cooldown 留 3.3 光束；Overload StatStack→MultiTarget；`_rayHeight`→`_grabHeight` |
| 2026-06-20 | M3.2 | 新增 PullAbility（Kind=Pull,Instant）+ PullTuning SO（grabRange/pullToDistance/hookRange/hookLandDistance）；普通拉吸向自己、超载抓钩（v1.0）RaycastAll 命中 Pushable→Teleport 玩家；复用 Pushable tag；AbilityKind+Pull、IAbilityContext+Controller；M3.md 验收同步 v1.0（抓钩） |
| 2026-06-20 | M3.4 | 新增 DashAbility（Kind=Dash,Instant）+ DashTuning SO（dashDistance/cooldown）；普通向移动输入方向突进、超载×2（J4：1 次触发算 1 次腐败）；RaycastAll 防穿墙；AbilityKind+Dash |
